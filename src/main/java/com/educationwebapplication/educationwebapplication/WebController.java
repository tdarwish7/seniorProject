package com.educationwebapplication.educationwebapplication;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class WebController {

/*
* This Java class is in charge of routing our pages to different urls'.
* When adding or creating a new web page. Add the code block below.
* Change the name of the url you'd like to use to route to, to the
* proper name of the web page and add a new HTML5 file in the templates
* folder.
*
*
    @RequestMapping("/home")
    public String home() {
        return "home";
    }*/
}
